#define CATCH_CONFIG_MAIN  // This tells Catch to provide a main() - only do this in one cpp file
#include "catch.h"

/**
 * This is the main test unit file.
 * Yet, it does nothing but compile and generate an automated test(main section).
 * Put all the other tests in individual (grouped per thema) cpp test files.
 */
